package main

// Expects a line with space broken parts
// Comes as normal assembly
// Expects S bit and ConditionCode to come after name of instruction
// Expects a space between the operation, the condition, and the s bit
// Should be in that order
func ParseInstruction(line string) (Instruction, error) {
	
}
